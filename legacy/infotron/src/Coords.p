unit Coords;{Coords - a unit that handles normalized, screen, and world coordinates.}{I wrote it because I couldn't understand anything else.}{Please don't be offended.}interface	type		realPt = record				h, v: real;			{to keep consistent with the mac data structures}			end;		realRect = record				case integer of					1: (							left, top, right, bottom: real					);					2: (							topLeft, botRight: realPt					);			end;		worldPt = realPt;		normPt = realPt;		worldRect = realRect;		normRect = realRect;		screenInfo = record				screenRect: rect;	{the total drawing window size, including portions not in the NDC range.}				usedRect: rect;		{what is actually used}			end;implementationend.